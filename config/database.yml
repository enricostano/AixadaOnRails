# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: postgresql
  encoder: utf8
  database: aixada_development
  pool: 5
  username: enrico
  password:

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoder: utf8
  database: aixada_test
  pool: 5
  username: enrico
  password:

production:
  adapter: postgresql
  encoder: utf8
  database: aixada
  pool: 5
  username: enrico
  password:
